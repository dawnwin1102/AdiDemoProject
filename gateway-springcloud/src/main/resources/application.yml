spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: routeportal
          uri: lb://routeportal
          predicates:
            - Path=/**